set the classpath in the makefile
Set the SPARK and SCALA installation paths

Input 4 is the dataset to run the code in(large or small)
Uncomment gzip lines if needed.
Place the small dataset directly in ./input/
Place the large dataset in ./input/large/
run the makefile
For report xtable library is required
Generate the jar and run the make command
(jar can be generated by adding command jar cvfm jar name,did not include it since for my machine,it runs sporadically) 
